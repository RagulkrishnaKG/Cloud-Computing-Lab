# Experiment 08 – Output

## Sample Output
The Docker commands display the installed Docker version, available images, and running or stopped containers. Running `hello-world` displays Docker's confirmation message.

## Result
Docker images and containers were inspected and executed using the commands in this experiment.
