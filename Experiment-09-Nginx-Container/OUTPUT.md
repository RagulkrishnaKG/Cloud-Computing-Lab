# Experiment 09 – Output

After running the container, open:

```text
http://localhost:8080
```

The browser displays the Nginx welcome page.

## Result
The Nginx web server was executed successfully inside a Docker container.
